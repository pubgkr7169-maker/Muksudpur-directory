'use client';
import {useState} from 'react';
import {LockKeyhole,Eye,EyeOff,ArrowLeft} from 'lucide-react';
import {Header,usePreferences} from '../shared';
export default function Login(){const prefs=usePreferences();const[password,setPassword]=useState(''),[visible,setVisible]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const v=await r.json() as {error?:string};if(!r.ok)throw Error(v.error||'Unable to sign in');window.location.assign('/admin')}catch(e){setError((e as Error).message);setBusy(false)}}return <><Header prefs={prefs} admin/><main className="shell" id="main"><a className="back-link" href="/"><ArrowLeft size={18}/>Back to directory</a><section className="login-card"><span className="service-icon"><LockKeyhole size={27}/></span><h1>Admin sign in</h1><p>Enter your password to add, edit, rename or delete contacts.</p><form onSubmit={submit}><label htmlFor="admin-password">Admin password</label><div className="password-input"><input id="admin-password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)} type={visible?'text':'password'} autoFocus maxLength={200}/><button type="button" onClick={()=>setVisible(!visible)} aria-label={visible?'Hide password':'Show password'}>{visible?<EyeOff size={22}/>:<Eye size={22}/>}</button></div>{error&&<p role="alert" className="error">{error}</p>}<button className="call-button" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><small>Your session stays signed in for up to 8 hours.</small></section></main></>}
